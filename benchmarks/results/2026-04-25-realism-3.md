# PindeX Realism Benchmark — 2026-04-25

- Model: `claude-sonnet-4-6`
- N runs per (task, condition): 1 (warm-up discarded)
- Order alternation: even-indexed tasks BASELINE→PINDEX, odd-indexed PINDEX→BASELINE
- Cost rates: input \$3/M, cache-read \$0.3/M, output \$15/M
- Total measured spend: \$1.500
- Claude CLI capabilities: {"hasMcpConfig":true,"hasAppendSystemPrompt":true,"appendSystemPromptFlag":"--append-system-prompt"}

**Conditions:** BASELINE = vanilla Claude Code (no PindeX MCP server, only native Read/Grep/Glob/Bash). PINDEX = same Claude Code with the PindeX MCP server registered (14 mcp__pindex__* tools available).

## Codebase: pindex

| Task | Baseline input | PindeX input | Ratio | Cache-read share (PindeX) | Output Δ |
|---|---:|---:|---:|---:|---:|
| **pindex-1** Where is the RepoSet class defined? List its public methods.… | 122.905 | 122.687 | 0.998 | 100% | +0 |
| **pindex-2** Which MCP tools accept the repos parameter? List them.… | 11.888 | 11.888 | 1.000 | 100% | +87 |
| **pindex-3** How does crash recovery work in LspPythonClient? Describe the st… | 122.801 | 123.254 | 1.004 | 100% | -19 |
| **pindex-4** Where is the assignName function called from? List all call site… | 79.014 | 94.567 | 1.197 | 100% | -50 |
| **pindex-5** What does src/indexer/index.ts import from other PindeX modules?… | 123.169 | 67.066 | 0.545 | 100% | -647 |
| **pindex-6** Explain how processParsedFile interacts with the AST diff engine… | 282.830 | 183.698 | 0.649 | 100% | -407 |
| **TOTAL** | 742.607 | 603.160 | **0.812** | 100% | -1036 |

## Codebase: typescript-eslint

| Task | Baseline input | PindeX input | Ratio | Cache-read share (PindeX) | Output Δ |
|---|---:|---:|---:|---:|---:|
| **tseslint-1** Where is the rule-creator helper (the function used to define an… | 104.311 | 80.353 | 0.770 | 100% | -182 |
| **tseslint-2** How many plugin packages does this monorepo contain? Where do th… | 80.461 | 80.605 | 1.002 | 100% | +29 |
| **tseslint-3** What test helpers exist under packages/eslint-plugin/tests? List… | 110.361 | 110.106 | 0.998 | 100% | +323 |
| **tseslint-4** Explain the type-resolver strategy: where is program.getTypeChec… | 139.029 | 135.844 | 0.977 | 100% | -724 |
| **tseslint-5** How is the TSConfig consumed by parseAndGenerateServices? Show t… | 410.279 | 279.136 | 0.680 | 100% | +138 |
| **tseslint-6** Where is the AST converted to TSESTree format?… | 104.407 | 93.425 | 0.895 | 100% | +24 |
| **TOTAL** | 948.848 | 779.469 | **0.821** | 100% | -392 |

## Conclusion

- **pindex**: PindeX reduces total input tokens by 19% (ratio 0.812; cache-read share 100%).
- **typescript-eslint**: PindeX reduces total input tokens by 18% (ratio 0.821; cache-read share 100%).

## Appendix: per-task answers (excerpts)

### pindex

#### pindex-1: Where is the RepoSet class defined? List its public methods.

**Baseline answer:** `RepoSet` is defined in `src/federation/repo-set.ts:20`.

**Public methods/accessors:**

| Name | Kind | Description |
|---|---|---|
| `static fromServerConfig(primaryDb, primaryName, federated, primaryPath?)` | static factory | Constructs a `RepoSet` from the server's DB config 

**PindeX answer:** **`RepoSet`** — defined in `src/federation/repo-set.ts:20`

Public methods/accessors:

| Name | Kind | Description |
|---|---|---|
| `static fromServerConfig(primaryDb, primaryName, federated, primaryPath?)` | static factory | Creates a `RepoSet` from server config; primary repo 

#### pindex-2: Which MCP tools accept the repos parameter? List them.

**Baseline answer:** From the CLAUDE.md:

> 9 read-only tools accept an optional `repos: string[]` param to scope to specific federated repos:

- `search_symbols`
- `find_usages`
- `get_symbol`
- `get_file_summary`
- `get_context`
- `get_dependencies`
- `get_project_overview`
- `search_docs`
- `get_d

**PindeX answer:** From the CLAUDE.md documentation, these 9 read-only tools accept an optional `repos: string[]` parameter to scope results to specific federated repos:

1. `search_symbols`
2. `find_usages`
3. `get_symbol`
4. `get_file_summary`
5. `get_context`
6. `get_dependencies`
7. `get_projec

#### pindex-3: How does crash recovery work in LspPythonClient? Describe the state transitions.

**Baseline answer:** Here's how crash recovery works in `LspPythonClient` (`src/indexer/lsp-python.ts`):

## State Machine

```
idle → starting → ready → idle → starting → ready
                       ↘              ↘
                     failed          failed
                       ↑
              

**PindeX answer:** Here's how crash recovery works in `LspPythonClient`:

## State Machine

```
idle → starting → ready ─── crash ──→ idle (restart #1) → starting → ready
                  │                                                  ↘ failed (if spawn fails)
                  └── crash (2nd 

#### pindex-4: Where is the assignName function called from? List all call sites.

**Baseline answer:** Here are all call sites of `assignName`:

**Production code:**

| File | Line | Context |
|---|---|---|
| `src/index.ts` | 57 | Primary repo name: `primaryEntry?.name ?? assignName(PROJECT_ROOT, usedNames)` |
| `src/index.ts` | 67 | Federated repo name: `name = assignName(repoPat

**PindeX answer:** `assignName` is called from **6 production call sites**:

| File | Line | Context |
|---|---|---|
| `src/cli/project-detector.ts` | 126 | Assigning name to a project `p` during registry load |
| `src/cli/project-detector.ts` | 181 | Assigning name to a newly normalized path |
| `

#### pindex-5: What does src/indexer/index.ts import from other PindeX modules? Give the dependency list.

**Baseline answer:** Full dependency list for `src/indexer/index.ts`:

| Module | Symbols imported |
|---|---|
| `../types.js` | `IndexResult`, `ParsedFile`, `ParsedImport` |
| `./parse-pool.js` | `ParsePool` |
| `./parser.js` | `parseFile`, `parseDocument`, `hashContent` |
| `../db/queries.js` | `up

**PindeX answer:** It looks like the `mcp__pindex__get_dependencies` tool needs your permission. Could you approve it, or alternatively I can fall back to reading the file directly?

#### pindex-6: Explain how processParsedFile interacts with the AST diff engine.

**Baseline answer:** I now have a complete picture. Here's how `processParsedFile` interacts with the AST diff engine:

---

## `processParsedFile` ↔ AST Diff Engine

### Call site (`src/indexer/index.ts:307`)

`processParsedFile` calls `computeAstDiff` **inside a SQLite transaction**, right before i

**PindeX answer:** Here's how `processParsedFile` interacts with the AST diff engine:

---

## `processParsedFile` → `computeAstDiff` interaction

### Call site

`processParsedFile` (`src/indexer/index.ts:253`) is the central post-parse step. Inside a **single SQLite transaction** it does:

1. `ups

### typescript-eslint

#### tseslint-1: Where is the rule-creator helper (the function used to define an ESLint rule, like RuleCreator or createRule) defined?

**Baseline answer:** **`RuleCreator`** is defined in `packages/utils/src/eslint-utils/RuleCreator.ts:67`.

Here's the structure:

- **`RuleCreator(urlCreator)`** (line 67) — a factory that takes a URL-builder function and returns `createNamedRule`. This is the main export used by plugin packages (e.g

**PindeX answer:** The rule-creator helper is defined in two places:

1. **`RuleCreator` function** — `packages/utils/src/eslint-utils/RuleCreator.ts:67`  
   This is the generic factory that takes a `urlCreator` and returns a typed `createRule` function.

2. **`createRule`** used across individual

#### tseslint-2: How many plugin packages does this monorepo contain? Where do they live?

**Baseline answer:** This monorepo contains **2 plugin packages**, both living under `packages/`:

| Package | Path |
|---|---|
| `eslint-plugin` | `packages/eslint-plugin/` |
| `eslint-plugin-internal` | `packages/eslint-plugin-internal/` |

`eslint-plugin` is the main public `@typescript-eslint/esl

**PindeX answer:** There are **2 plugin packages**, both under `packages/`:

| Directory | Description |
|---|---|
| `packages/eslint-plugin/` | The main public plugin (`@typescript-eslint/eslint-plugin`) |
| `packages/eslint-plugin-internal/` | An internal plugin used for linting the repo itself |

#### tseslint-3: What test helpers exist under packages/eslint-plugin/tests? List the top-level classes and exported functions.

**Baseline answer:** Here are the test helpers under `packages/eslint-plugin/tests`:

---

### `RuleTester.ts`
Two exported functions:

| Function | Signature | Purpose |
|---|---|---|
| `createRuleTesterWithTypes` | `(providedParserOptions?: ParserOptions) → RuleTester` | Creates a `RuleTester` wire

**PindeX answer:** Here are the test helpers under `packages/eslint-plugin/tests`:

---

### Top-level helpers

**`tests/RuleTester.ts`**
- `createRuleTesterWithTypes(providedParserOptions?)` — builds a `RuleTester` pre-configured with type-aware parsing (`projectService` enabled by default, disabl

#### tseslint-4: Explain the type-resolver strategy: where is program.getTypeChecker() invoked?

**Baseline answer:** Here is the complete picture.

## Type-resolver strategy: where `program.getTypeChecker()` is called

There are **two distinct reasons** the call appears, at two different layers.

---

### 1. Side-effect-only calls — program creation layer

Three locations in `packages/typescrip

**PindeX answer:** Here is a full picture of the type-resolver strategy:

---

## Where `program.getTypeChecker()` is invoked and why

There are **two distinct purposes** for calling `getTypeChecker()`, and they happen in different layers.

### 1. Side-effect: forcing parent-pointer initialisation 

#### tseslint-5: How is the TSConfig consumed by parseAndGenerateServices? Show the relevant call graph.

**Baseline answer:** Now I have the full picture. Here's the call graph:

---

## TSConfig Call Graph in `parseAndGenerateServices`

```
parseAndGenerateServices(code, tsestreeOptions)          [parser.ts:158]
│
├─ createParseSettings(code, tsestreeOptions)             [createParseSettings.ts:47]
│  

**PindeX answer:** Here is the full call graph showing how TSConfig is consumed by `parseAndGenerateServices`:

---

## TSConfig Call Graph

```
parseAndGenerateServices(code, tsestreeOptions)        parser.ts:158
│
├─ createParseSettings(code, tsestreeOptions)          parseSettings/createParseSet

#### tseslint-6: Where is the AST converted to TSESTree format?

**Baseline answer:** The conversion happens in `packages/typescript-estree/src/convert.ts`. The key entry point is `convertNode` at line 657, which is a private method of the `Converter` class. The public entry is at line 490 where it calls `this.convertNode(node, parentNode)`.

- **`convert.ts:657`*

**PindeX answer:** The conversion happens in two layers:

1. **`packages/typescript-estree/src/convert.ts:69`** — The `Converter` class is the core of the conversion. Its `convertProgram()` method (line 2854) walks the TypeScript AST and converts each `ts.Node` into a `TSESTree` node.

2. **`packag

# PindeX Realism Benchmark — 2026-04-25

- Model: `claude-sonnet-4-6`
- N runs per (task, condition): 1 (warm-up discarded)
- Order alternation: even-indexed tasks BASELINE→PINDEX, odd-indexed PINDEX→BASELINE
- Cost rates: input \$3/M, cache-read \$0.3/M, output \$15/M
- Total measured spend: \$0.305
- Claude CLI capabilities: {"hasMcpConfig":true,"hasAppendSystemPrompt":true,"appendSystemPromptFlag":"--append-system-prompt"}

**Conditions:** BASELINE = vanilla Claude Code (no PindeX MCP server, only native Read/Grep/Glob/Bash). PINDEX = same Claude Code with the PindeX MCP server registered (14 mcp__pindex__* tools available).

## Codebase: pindex

| Task | Baseline input | PindeX input | Ratio | Cache-read share (PindeX) | Output Δ |
|---|---:|---:|---:|---:|---:|
| **pindex-1** Where is the RepoSet class defined? List its public methods.… | 39.097 | 39.097 | 1.000 | 100% | +84 |
| **pindex-2** Which MCP tools accept the repos parameter? List them.… | 11.888 | 11.888 | 1.000 | 100% | +98 |
| **pindex-3** How does crash recovery work in LspPythonClient? Describe the st… | 39.102 | 39.102 | 1.000 | 100% | -98 |
| **pindex-4** Where is the assignName function called from? List all call site… | 39.097 | 66.456 | 1.700 | 100% | +20 |
| **pindex-5** What does src/indexer/index.ts import from other PindeX modules?… | 39.107 | 153.298 | 3.920 | 100% | +507 |
| **pindex-6** Explain how processParsedFile interacts with the AST diff engine… | 39.100 | 39.100 | 1.000 | 100% | -82 |
| **TOTAL** | 207.391 | 348.941 | **1.683** | 100% | +529 |

## Conclusion

- **pindex**: PindeX increases total input tokens by +-68% (ratio 1.683; cache-read share 100%).

## Appendix: per-task answers (excerpts)

### pindex

#### pindex-1: Where is the RepoSet class defined? List its public methods.

**Baseline answer:** **File:** `src/federation/repo-set.ts`

**Public methods:**

| Method | Signature |
|---|---|
| `fromServerConfig` | `static fromServerConfig(primaryDb, primaryName, federated, primaryPath?): RepoSet` |
| `filter` | `filter(repos?: string[]): Repo[]` |
| `primary` | `get primary(

**PindeX answer:** The `RepoSet` class is defined in `src/federation/repo-set.ts:20`.

**Public methods:**

| Method | Line | Description |
|--------|------|-------------|
| `static fromServerConfig(primaryDb, primaryName, federated, primaryPath?)` | 27 | Static factory — constructs a `RepoSet` fro

#### pindex-2: Which MCP tools accept the repos parameter? List them.

**Baseline answer:** From the CLAUDE.md:

> 9 read-only tools accept an optional `repos: string[]` param to scope to specific federated repos:

- `search_symbols`
- `find_usages`
- `get_symbol`
- `get_file_summary`
- `get_context`
- `get_dependencies`
- `get_project_overview`
- `search_docs`
- `get_d

**PindeX answer:** From the CLAUDE.md, these 9 read-only tools accept an optional `repos: string[]` parameter to scope results to specific federated repos:

1. `search_symbols`
2. `find_usages`
3. `get_symbol`
4. `get_file_summary`
5. `get_context`
6. `get_dependencies`
7. `get_project_overview`
8.

#### pindex-3: How does crash recovery work in LspPythonClient? Describe the state transitions.

**Baseline answer:** Here's how crash recovery works in `LspPythonClient`:

## State Machine

States: `'idle' | 'starting' | 'ready' | 'failed' | 'closed'`

```
idle ──start()──> starting ──init OK──> ready
                      │                   │
                   spawn/init fail         │  proc

**PindeX answer:** Here's how crash recovery works in `LspPythonClient` (`src/indexer/lsp-python.ts`):

## States

Five possible states (line 16):
- `idle` — initial / post-reset
- `starting` — subprocess spawned, handshake in progress
- `ready` — fully initialized, accepting requests
- `failed` — 

#### pindex-4: Where is the assignName function called from? List all call sites.

**Baseline answer:** `assignName` is defined in `src/federation/registry-name.ts:8` and called from **4 production files** plus tests:

| File | Line(s) | Context |
|---|---|---|
| `src/cli/project-detector.ts` | 126, 181 | Assigning name to registered project / normalizing path |
| `src/cli/init.ts`

**PindeX answer:** Here are all the call sites of `assignName` in the source code:

**`src/cli/project-detector.ts`**
- Line 126 — backfilling missing names in existing registry entries
- Line 181 — assigning a name when adding a new project

**`src/cli/init.ts`**
- Line 364 — used via dynamic impo

#### pindex-5: What does src/indexer/index.ts import from other PindeX modules? Give the dependency list.

**Baseline answer:** Here are the PindeX module imports for `src/indexer/index.ts`:

**Project-internal dependencies:**

| Module | Symbols |
|---|---|
| `../types.js` | `IndexResult`, `ParsedFile`, `ParsedImport` |
| `./parse-pool.js` | `ParsePool` |
| `./parser.js` | `parseFile`, `parseDocument`, `

**PindeX answer:** Here are the PindeX-internal imports from `src/indexer/index.ts`:

| Module | Symbols |
|---|---|
| `../types.js` | `IndexResult`, `ParsedFile`, `ParsedImport` (type-only) |
| `./parse-pool.js` | `ParsePool` |
| `./parser.js` | `parseFile`, `parseDocument`, `hashContent` |
| `../

#### pindex-6: Explain how processParsedFile interacts with the AST diff engine.

**Baseline answer:** Here's the complete interaction chain:

## `processParsedFile` ↔ AST Diff Engine

### Where the interaction happens

`processParsedFile` (`src/indexer/index.ts:253`) calls `computeAstDiff` (`src/memory/ast-diff.ts:49`) **inside the same SQLite transaction** that re-writes the sym

**PindeX answer:** Here's how `processParsedFile` and the AST diff engine interact:

## Pipeline Overview

```
indexFile() → processParsedFile() → computeAstDiff() → AstDiffResult
                                                              ↓
                                               FileWatc

# PindeX Realism Benchmark — 2026-06-13

- Model: `claude-sonnet-4-6`
- N runs per (task, condition): 1 (warm-up discarded)
- Order alternation: even-indexed tasks BASELINE→PINDEX, odd-indexed PINDEX→BASELINE
- Cost rates: input \$3/M, cache-read \$0.3/M, output \$15/M
- Total measured spend: \$0.206
- Claude CLI capabilities: {"hasMcpConfig":true,"hasAppendSystemPrompt":true,"appendSystemPromptFlag":"--append-system-prompt"}

**Conditions:** BASELINE = vanilla Claude Code (no PindeX MCP server, only native Read/Grep/Glob/Bash). PINDEX = same Claude Code with the PindeX MCP server registered (14 mcp__pindex__* tools available).

## Codebase: pindex

| Task | Baseline input | PindeX input | Ratio | Cache-read share (PindeX) | Output Δ |
|---|---:|---:|---:|---:|---:|
| **pindex-1** Where is the RepoSet class defined? List its public methods.… | 65.656 | 167.336 | 2.549 | 100% | +236 |
| **pindex-2** Which MCP tools accept the repos parameter? List them.… | 32.827 | 17.151 | 0.522 | 100% | -52 |
| **pindex-3** How does crash recovery work in LspPythonClient? Describe the st… | 49.977 | 65.654 | 1.314 | 100% | +214 |
| **TOTAL** | 148.460 | 250.141 | **1.685** | 100% | +398 |

## Conclusion

- **pindex**: PindeX increases total input tokens by 68% (ratio 1.685; cache-read share 100%).

## Appendix: per-task answers (excerpts)

### pindex

#### pindex-1: Where is the RepoSet class defined? List its public methods.

**Baseline answer:** `RepoSet` is defined in `src/federation/repo-set.ts:20` and has these public members:

| Name | Type | Signature |
|---|---|---|
| `fromServerConfig` | static method | `(primaryDb, primaryName, federated, primaryPath?) → RepoSet` |
| `filter` | method | `(repos?: string[]) → Repo

**PindeX answer:** `RepoSet` is defined in `src/federation/repo-set.ts:20`.

**Public API:**

| Member | Type | Description |
|---|---|---|
| `static fromServerConfig(primaryDb, primaryName, federated, primaryPath?)` | static factory | Creates a `RepoSet` from a primary DB + federated repo configs 

#### pindex-2: Which MCP tools accept the repos parameter? List them.

**Baseline answer:** From the CLAUDE.md in this project:

> 9 read-only tools accept an optional `repos: string[]` param to scope to specific federated repos:

- `search_symbols`
- `find_usages`
- `get_symbol`
- `get_file_summary`
- `get_context`
- `get_dependencies`
- `get_project_overview`
- `searc

**PindeX answer:** From the CLAUDE.md:

> 9 read-only tools accept an optional `repos: string[]` param to scope to specific federated repos:

- `search_symbols`
- `find_usages`
- `get_symbol`
- `get_file_summary`
- `get_context`
- `get_dependencies`
- `get_project_overview`
- `search_docs`
- `get_d

#### pindex-3: How does crash recovery work in LspPythonClient? Describe the state transitions.

**Baseline answer:** `LspPythonClient` lives in `src/indexer/lsp-python.ts` and has a single-retry crash recovery with two triggers.

---

## State type

```typescript
type LspReadyState = 'idle' | 'starting' | 'ready' | 'failed' | 'closed'
```

---

## Normal startup

```
idle → starting → ready
```

**PindeX answer:** `LspPythonClient` lives in `src/indexer/lsp-python.ts` and uses a 5-state enum (`idle → starting → ready → failed / closed`) with a single-retry crash recovery policy.

## State Machine

```
idle ──start()──> starting ──init OK──> ready
                      │                   │
